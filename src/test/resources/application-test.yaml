spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/test-challenge-neurotech
    driver-class-name: org.h2.Driver
    username: test
    password: test
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create-drop